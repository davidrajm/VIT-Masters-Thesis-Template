%%
\usepackage{graphicx, amsmath, times, enumitem, amssymb}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
%%
\usepackage{makeidx}
\makeindex

\usepackage{natbib}

%%%--- Custom commands for use case
\newcommand{\thesistitle}[1]%
{\def\printtitle{#1} \title{#1}}%

\newcommand{\student}[1]%
{\def\studentname{#1}\author{#1}}%

\newcommand{\regno}[1]%
{\def\studentregno{#1}}%

\newcommand{\guide}[1]%
{\def\vguide{#1}}%

\newcommand{\submittedon}[1]{
\def\printsubmittedon{#1}
}

\newenvironment{keywords}{\noindent\textbf{Keywords:}}{}



%%%%%%%------------------------------------------------
%%%%%%-----------Page Layout Setups------
%%%%%%--------------------------------------------------
\usepackage{geometry}
\geometry{
	headheight=6ex,
	includehead,
	includefoot
}
\geometry{
	paper=a4paper,% Change to letterpaper for US letter
	inner=1in, % Inner margin
	outer=1in, % Outer margin
	bindingoffset=0cm, % Binding offset
	top=1in, % Top margin
	bottom=1in, % Bottom margin
	%showframe,% show how the type block is set on the page
}
\renewcommand{\baselinestretch}{1.5} % for line spacing
\usepackage{fancyhdr}%for the header and footer
\usepackage{emptypage}%to remove headers and footers in the blank pages	

\pagestyle{fancy} %Initiates the Page Style

\fancyhf{}
\renewcommand{\headrulewidth}{1pt}
%\fancyhead[L]{\nouppercase \printtitle}
\fancyhead[R]{\nouppercase \leftmark}

\renewcommand{\footrulewidth}{0.5pt}
\fancyfoot[R]{\thepage}
\fancyfoot[L]{
%	{\scriptsize Divison of Mathematics, School of Advanced Sciences, Vellore Institute of Technology ,  Chennai Campus}
}



%%%%%------------------------------------------------
%%%%%-----------Hyperlinks Setup---------------------
%%%%%------------------------------------------------
\usepackage{nameref}
\usepackage[pdfusetitle]{hyperref}
\hypersetup{
	colorlinks=true,
	linkcolor=black,
	filecolor=red,      
	urlcolor=black,
	citecolor = black
}

\usepackage[english]{babel}
\usepackage[utf8]{inputenc}
\addto\extrasenglish{%
	\renewcommand{\chapterautorefname}{Chapter}%
}
\addto\extrasenglish{%
	\renewcommand{\sectionautorefname}{Section}%
}



%----------------------------------------------------------------------------------------
%	PENALTIES( for the Hypenation Problem)
%----------------------------------------------------------------------------------------

\doublehyphendemerits=10000 % No consecutive line hyphens
\brokenpenalty=10000 % No broken words across columns/pages
\widowpenalty=9999 % Almost no widows at bottom of page
\clubpenalty=9999 % Almost no orphans at top of page
\interfootnotelinepenalty=9999 % Almost never break footnotes


\bibliographystyle{agsm}
\addcontentsline{toc}{chapter}{References}